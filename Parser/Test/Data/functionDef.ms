/* function_def */
function varname = expr
fn varname = expr

mapped function varname arg = expr
mapped fn varname arg = expr

fn varname arg1 arg2:#(expr, expr) arg3: &arg4: = expr

fn varname = (
  expr

  return expr
)

fn varname arg1 arg2:#(expr, expr) arg3: &arg4: = (
  expr

  return expr
)
